[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "requests"
version = "0.0.1"
description = "A minimal test package"
requires-python = ">=3.8"

[tool.setuptools]
py-modules = ["requests_stub"]
